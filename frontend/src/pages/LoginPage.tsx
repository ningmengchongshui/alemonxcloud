export function LoginPage({ error, onLogin, onDevLogin }: { error: string; onLogin: () => void; onDevLogin: () => void }) { return <main className="auth"><section className="auth-art"><div className="brand"><span className="logo">◢</span>AlemonX <small>CLOUD</small></div><div><p className="eyebrow">自建云平台</p><h1>把 AlemonX<br />部署到你的<span>裸机</span>上。</h1><p className="lead">从镜像到运行实例，只需一次选择。</p></div></section><section className="login"><div className="login-card"><p className="eyebrow">欢迎使用</p><h2>登录你的控制台</h2><p className="muted">通过 AlemonX Auth 安全登录。</p><button className="oauth" onClick={onLogin}><b>◉</b>使用 AlemonX Auth 继续</button>{import.meta.env.DEV && <button className="oauth dev-login" onClick={onDevLogin}>开发模式：以内置超级管理员进入</button>}{error && <p className="login-error" role="alert">{error}</p>}<p className="login-note">登录方式由认证中心统一管理。</p></div></section></main> }
