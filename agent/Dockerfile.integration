FROM golang:1.24-alpine AS builder
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /xcloud-agent .

FROM alpine:3.20
RUN apk add --no-cache docker-cli
COPY --from=builder /xcloud-agent /usr/local/bin/xcloud-agent
CMD ["xcloud-agent", "--serve"]
