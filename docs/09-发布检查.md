# 发布检查

## 配置与网络

- [ ] 生产使用 `GIN_MODE=release`，OIDC 回调地址完全一致。
- [ ] MySQL、Redis、RabbitMQ 已持久化、备份且可访问。
- [ ] 已备份 MySQL，并确认 `xcloud_schema_migrations` 中迁移版本、名称和校验和正确。
- [ ] 加密密钥和监控令牌已安全保存，未进入 Git。
- [ ] Agent 已安装为 systemd 服务，每台节点使用独立令牌。
- [ ] 13092 仅允许控制面网段和本机；DNS、TLS、两份 Nginx 配置已生效。

## 业务演练

- [ ] 已核对镜像摘要、套餐和节点配额。
- [ ] 完成登录 → 钱包购买 → 部署 → 域名访问 → 钱包续费 → 退款试算与确认。
- [ ] 验证任务重试、容器 404、消费者租约恢复、节点离线后恢复、余额不足和容量不足路径。
- [ ] 验证用户提交工单、管理员回复/关闭、用户重新打开及他人无权访问路径。
- [ ] 验证退款不立即停止、后续续费服务期前移、普通到期 7 天保留、退款到期 30 天保留及 7 天/1 天站内提醒。

## 运行保障

- [ ] `/healthz`、`/readyz`、`/metrics` 已接入监控。
- [ ] RabbitMQ、Agent、任务积压、失败任务、部署失败、运行资源丢失、销毁受阻、离线节点影响实例、租约重复恢复和磁盘告警已验证。
- [ ] 完成应用、RabbitMQ 和 Agent 重启恢复演练。
- [ ] 已在隔离环境运行 `make integration-up && make test-integration && make integration-down`。
